#include <stdio.h>
int main()
{
    int a, b, sum;

    scanf("%d", &a); //Inter a integer number
    scanf("%d", &b); //Inter a integer number

    sum = a + b;     //Instruction of adding this two number

    printf("Sum is: %d\n", sum);  //Print result of Addition
    return 0;
}
